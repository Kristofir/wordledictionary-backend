import { WordResult } from "@models/WordResult";

function createWordResult(
  word: Word,
  overlap: boolean,
  possibleAnswer: boolean,
  eliminations: number
) {
  const wordResult = {
    word: word,
    overlap: false,

  }
}